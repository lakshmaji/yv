---
"yv": patch
---

Docs: give `CLAUDE.md` back the orientation it needs after the slim-down — what
each `internal/` package owns, how the frontend talks to Go, and the invariants
of the config, scanning and import paths — and state the goal the format serves.
Also corrects `FEATURES.md` and one README bullet, which still offered "JSON or
YAML" export after YAML became the only format.
