{
  "name": "yv",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Version anchor for changesets. The app itself is Go + Wails; frontend/package.json is the web build's manifest and is deliberately not a workspace member here.",
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version && node scripts/sync-version.mjs",
    "sync-version": "node scripts/sync-version.mjs"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.1",
    "@changesets/cli": "^2.29.7"
  }
}
